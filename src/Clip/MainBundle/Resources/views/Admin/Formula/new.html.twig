{% extends 'ClipMainBundle:Admin:layout.html.twig' %}

{% block main -%}
    <h1>Formula creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('formula') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
