{% extends 'ClipMainBundle:Admin:layout.html.twig' %}

{% block main -%}
    <h1>Subscription</h1>

    <table class="show">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Participantid</th>
                <td>{{ entity.participantId }}</td>
            </tr>
            <tr>
                <th>Formulaid</th>
                <td>{{ entity.formulaId }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('subscription') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('subscription_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
